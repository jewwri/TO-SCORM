[project]
name = "scorm-support"
version = "1.0.0"
description = "Support metadata for the static SCORM generator repository."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
